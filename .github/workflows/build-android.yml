name: Build Android APK

# The Godot project lives in app/ (the repo root holds RE tooling + docs).
# All Godot commands run from app/; the engine binary is downloaded to the repo
# root and referenced by absolute path ($GITHUB_WORKSPACE/godot).

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

env:
  GODOT_VERSION: 4.6.2
  EXPORT_NAME: pm98

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install required SDK packages
        run: |
          set -e
          sdkmanager --install "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Download Godot ${{ env.GODOT_VERSION }} (Linux headless)
        run: |
          set -e
          URL="https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          wget -q "$URL"
          unzip -q "Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          mv "Godot_v${GODOT_VERSION}-stable_linux.x86_64" godot
          chmod +x godot
          ./godot --version

      - name: Download Godot Android export templates
        run: |
          set -e
          URL="https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_export_templates.tpz"
          wget -q "$URL"
          TPL_DIR="$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.stable"
          mkdir -p "$TPL_DIR"
          unzip -q "Godot_v${GODOT_VERSION}-stable_export_templates.tpz"
          mv templates/* "$TPL_DIR/"
          ls "$TPL_DIR" | head -20

      # RELEASE signing (2026-07-27). If the three ANDROID_RELEASE_* secrets are set the
      # build produces a RELEASE-signed APK; with them unset it falls back to the
      # debug-signed one exactly as before, so the pipeline never breaks on a fork or a
      # clone. Creating the keystore is deliberately NOT automated -- a release key is the
      # owner's to hold, and one generated in CI would be worthless (a fresh key every run
      # means every update is a different app to Android). To turn it on, once:
      #   keytool -genkeypair -v -keystore pm98-release.keystore -alias pm98 \
      #     -keyalg RSA -keysize 4096 -validity 10000 -storetype PKCS12
      #   base64 -w0 pm98-release.keystore     # -> secret ANDROID_RELEASE_KEYSTORE_BASE64
      # plus ANDROID_RELEASE_KEY_ALIAS and ANDROID_RELEASE_KEYSTORE_PASSWORD.
      # KEEP THE .keystore FILE. Lose it and the app can never be updated in place.
      - name: Restore release keystore (optional)
        env:
          REL_B64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
        run: |
          set -e
          if [ -n "$REL_B64" ]; then
            echo -n "$REL_B64" | base64 -d > "$GITHUB_WORKSPACE/release.keystore"
            echo "BUILD_MODE=release" >> "$GITHUB_ENV"
            echo "Release keystore restored - this build will be RELEASE-signed."
          else
            echo "BUILD_MODE=debug" >> "$GITHUB_ENV"
            echo "No ANDROID_RELEASE_KEYSTORE_BASE64 secret - falling back to a debug-signed APK."
          fi

      - name: Restore (or generate) debug keystore
        env:
          KEYSTORE_B64: ${{ secrets.ANDROID_DEBUG_KEYSTORE_BASE64 }}
        run: |
          set -e
          if [ -n "$KEYSTORE_B64" ]; then
            echo "Decoding stable keystore from ANDROID_DEBUG_KEYSTORE_BASE64 secret"
            echo -n "$KEYSTORE_B64" | base64 -d > "$GITHUB_WORKSPACE/debug.keystore"
          else
            echo "Secret not set — generating fresh ephemeral keystore (signature will change per build)"
            keytool -genkeypair -v \
              -keystore "$GITHUB_WORKSPACE/debug.keystore" \
              -alias androiddebugkey \
              -storepass android -keypass android \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US" \
              -storetype PKCS12
          fi
          keytool -list -keystore "$GITHUB_WORKSPACE/debug.keystore" -storepass android | grep -E "fingerprint|Keystore type" | head -3

      - name: Configure Godot editor settings (SDK + keystore paths)
        run: |
          set -e
          CFG_DIR="$HOME/.config/godot"
          mkdir -p "$CFG_DIR"
          GODOT_MINOR=$(echo "$GODOT_VERSION" | cut -d. -f1-2)
          SETTINGS_FILE="$CFG_DIR/editor_settings-${GODOT_MINOR}.tres"
          cat > "$SETTINGS_FILE" <<EOF
          [gd_resource type="EditorSettings" format=3]

          [resource]
          export/android/java_sdk_path = "$JAVA_HOME"
          export/android/android_sdk_path = "$ANDROID_HOME"
          export/android/debug_keystore = "$GITHUB_WORKSPACE/debug.keystore"
          export/android/debug_keystore_user = "androiddebugkey"
          export/android/debug_keystore_pass = "android"
          EOF
          echo "Wrote $SETTINGS_FILE (mode: $BUILD_MODE)"
          cat "$SETTINGS_FILE"
          # NOTE: there is no `export/android/release_keystore` EDITOR setting in Godot
          # 4.6 (only the three debug ones exist; verified against the shipped binary).
          # The release key is supplied to the exporter through its own environment
          # variables instead -- GODOT_ANDROID_KEYSTORE_RELEASE_{PATH,USER,PASSWORD} --
          # which also keeps the password out of any file on the runner.

      # Using the prebuilt export templates (gradle_build/use_gradle_build=false), so no
      # Android Gradle build template install is needed — the bundled android_debug.apk /
      # android_release.apk template is repackaged and signed with the matching keystore.

      - name: Pre-import project resources
        working-directory: app
        run: |
          "$GITHUB_WORKSPACE/godot" --headless --import . || true
          "$GITHUB_WORKSPACE/godot" --headless --import .

      - name: Script compile guard (never ship a grey/broken APK)
        working-directory: app
        run: |
          # Boot the real project headless (autoloads registered, main scene loaded) and
          # fail the build if ANY script fails to parse/compile. A broken main script
          # loads as a grey screen on device, which an import-only check does not catch.
          set -e
          LOG="$("$GITHUB_WORKSPACE/godot" --headless --quit-after 60 . 2>&1 || true)"
          echo "$LOG" | tail -30
          if echo "$LOG" | grep -qiE "SCRIPT ERROR|Parse Error|Compile Error|Failed to load script"; then
            echo "::error::GDScript parse/compile error detected — refusing to build a broken APK"
            exit 1
          fi
          echo "Scripts compile clean."

      - name: Headless test gate (core model + screen logic)
        working-directory: app
        run: |
          # A curated, deterministic subset of the 225-file suite: fast (<3 min all
          # told, timed 2026-07-27), display-free, and green at head. S3 landed
          # 2026-07-27 (one persisted career RNG stream), so test_pyramid's old flake
          # is gone and it gates again, alongside the new test_career_seed. The full
          # sweep stays a manual pre-release step (docs/REMAINING.md).
          set -e
          FAIL=0
          for t in test_career test_refrun_findings test_league_calendar \
                   test_contract_warning test_three_up_front_seam test_knockout_layout \
                   test_finance test_finance_detail test_finance_screen \
                   test_finance_control test_insurance test_insurance_screen \
                   test_cupdraw_screen test_cup_draw_then_play test_results_screen \
                   test_tactics_screen test_cheats_live \
                   test_stadium_screen test_stadium_works \
                   test_youth_loop test_career_seed test_pyramid \
                   test_retirement test_sacking test_weekly_illness \
                   test_mantoman; do
            echo "== $t"
            if ! "$GITHUB_WORKSPACE/godot" --headless --path . --script "res://tests/$t.gd" \
                > "/tmp/$t.log" 2>&1; then
              tail -25 "/tmp/$t.log"
              echo "::error::$t FAILED"
              FAIL=1
            fi
          done
          exit $FAIL

      - name: Diagnostic dump
        run: |
          echo "--- ~/.config/godot listing ---"
          ls -la "$HOME/.config/godot/" || true
          echo "--- all editor_settings*.tres ---"
          for f in "$HOME/.config/godot/"editor_settings*.tres; do
            echo ">>> $f"; cat "$f" || true; echo
          done
          echo "--- export templates dir ---"
          ls -la "$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.stable/" || true

      - name: Diagnose export config
        working-directory: app
        run: |
          echo "--- export_presets.cfg (head) ---"; head -20 export_presets.cfg
          echo "--- export templates dir ---"
          ls "$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.stable/" | grep -i android || true
          echo "--- keystore ---"; ls -la "$GITHUB_WORKSPACE/debug.keystore" || true
          echo "--- SDK build-tools (apksigner/zipalign) ---"
          ls "$ANDROID_HOME/build-tools/" 2>/dev/null || true

      - name: Export Android APK (release-signed when the secret is set)
        working-directory: app
        env:
          GODOT_ANDROID_KEYSTORE_RELEASE_USER: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
          GODOT_ANDROID_KEYSTORE_RELEASE_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEYSTORE_PASSWORD }}
        run: |
          set -o pipefail
          if [ "$BUILD_MODE" = "release" ]; then
            EXPORT_FLAG="--export-release"
            export GODOT_ANDROID_KEYSTORE_RELEASE_PATH="$GITHUB_WORKSPACE/release.keystore"
          else
            EXPORT_FLAG="--export-debug"
          fi
          echo "Exporting with $EXPORT_FLAG"
          "$GITHUB_WORKSPACE/godot" --headless --verbose "$EXPORT_FLAG" "Android" "${EXPORT_NAME}.apk" 2>&1 | tee export.log
          rc=$?
          echo "--- export.log error/warning lines ---"
          grep -iE "error|warning|template|keystore|sdk|missing|invalid|config|not found|valid" export.log | head -60 || true
          ls -la "${EXPORT_NAME}.apk"
          # Cache-bust (gotcha #12): the rolling latest/<name>.apk URL is heavily cached by
          # Android/browsers, so a fixed filename keeps serving the OLD build. Give every build a
          # unique, uncacheable filename (commit SHA suffix). Keep pm98.apk too for the artifact.
          APK_FILE="${EXPORT_NAME}-${GITHUB_SHA:0:7}.apk"
          cp "${EXPORT_NAME}.apk" "$APK_FILE"
          echo "APK_FILE=$APK_FILE" >> "$GITHUB_ENV"
          ls -la "$APK_FILE"
          exit $rc

      - name: Upload APK as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.EXPORT_NAME }}-apk
          path: app/${{ env.EXPORT_NAME }}.apk
          retention-days: 30

      - name: Update rolling "latest" pre-release (main pushes)
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest build (rolling)
          prerelease: true
          body: |
            Auto-built APK from latest `main` commit (${{ github.sha }}).
            Sideload only — signed with the project key when one is configured,
            otherwise debug-signed.

            **Install on phone:** download `${{ env.APK_FILE }}` (filename changes every
            build so your browser/Android can't serve a cached old APK), open it, allow
            "Install from this source" if Android prompts. If reinstalling over a previous
            build with a different signature, uninstall first.

            Ships with sample data only. Side-load your own `game_db.json`
            (built from owned game files) into the app's user data dir for the
            full database.
          files: app/${{ env.APK_FILE }}
          fail_on_unmatched_files: true

      # The APK filename carries the commit SHA (cache-busting, above), so any link
      # written into README.md by hand is stale the moment the next build lands. This
      # NOTE: GitHub honours [skip ci] ANYWHERE in a commit message, including the body.
      # Do not quote the token in a normal commit message describing this step, or that
      # push silently builds nothing and the README is not bumped.
      #
      # rewrites it to the APK this run just uploaded and pushes with [skip ci], so the
      # README download link is always the newest build and the commit does not rebuild.
      # The website (web/) resolves the newest asset from the API at page load instead;
      # a GitHub README cannot run JavaScript, so it needs this.
      - name: Point README at the APK this build produced (main pushes)
        if: github.ref == 'refs/heads/main'
        env:
          APK_FILE: ${{ env.APK_FILE }}
        run: |
          set -euo pipefail
          # Work off the current tip of main, not this run's (possibly older) checkout,
          # so a concurrent push is a failed push rather than a silent revert.
          git fetch --depth=1 origin main
          git checkout -B _readme_bump FETCH_HEAD
          python3 - "$APK_FILE" <<'EOF'
          import re, sys
          apk = sys.argv[1]
          s = open('README.md', encoding='utf-8').read()
          out = re.sub(r'pm98-[0-9a-f]{7,40}\.apk', apk, s)
          open('README.md', 'w', encoding='utf-8').write(out)
          print('README unchanged' if out == s else 'README -> ' + apk)
          EOF
          if git diff --quiet -- README.md; then
            echo "README already points at ${APK_FILE}"; exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -qam "README: download link -> ${APK_FILE} [skip ci]"
          # A losing race just means the next build corrects it; never fail the build.
          git push origin _readme_bump:main \
            || echo "push rejected (main moved) - the next build will correct the README"

      - name: Create tagged release (vX.Y.Z tags)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body: |
            Release ${{ github.ref_name }}.
            Sideload only — signed with the project key when one is configured,
            otherwise debug-signed.
          files: app/${{ env.EXPORT_NAME }}.apk
          fail_on_unmatched_files: true
