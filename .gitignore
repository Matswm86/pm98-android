# ── Copyrighted game data — Dinamic Multimedia. Personal use only, NEVER committed.
extracted/
assets/
*.iso
*.rar
*.PKF
*.pkf
*.DBC
*.30

# ── Third-party community materials (PC Fútbol editor manuals/help) — not ours to ship.
docs/refs/
docs/shots/

# ── Wine / scratch / Python
.wineprefix/
**/__pycache__/
*.pyc
*.pyo
.DS_Store

# ── Godot engine cache + generated import files (regenerated by CI on --import)
app/.godot/
**/*.import
*.translation
*.uid

# ── Android build artifacts (Gradle template installed in CI from export templates)
*.apk
*.aab
app/android/
app/export.log
app/shots/

# NOTE: the game database (app/data/game_db.json) and the decoded crests/photos/
# audio under app/ ARE committed on purpose — this is a public full-game clone of
# Mats's own PM98 (per owner decision 2026-06-14). Only the raw source game files
# (extracted/, *.rar, *.iso, *.PKF) stay out of git.
