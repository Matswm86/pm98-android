# ── Copyrighted game data — Dinamic Multimedia. Personal use only, NEVER committed.
extracted/
assets/
*.iso
*.rar
*.PKF
*.pkf
*.DBC
*.30

# ── Third-party community materials (PC Fútbol editor manuals/help) — not ours to ship.
docs/refs/
docs/shots/

# ── Wine / scratch / Python
.wineprefix/
**/__pycache__/
*.pyc
*.pyo
.DS_Store

# ── Godot engine cache + generated import files (regenerated by CI on --import)
app/.godot/
**/*.import
*.translation
*.uid

# ── Android build artifacts (Gradle template installed in CI from export templates)
*.apk
*.aab
app/android/
app/export.log

# ── The full game database is derived from owned game files → copyright, never
# committed. The synthetic app/data/sample_db.json fallback IS committed.
app/data/game_db.json
